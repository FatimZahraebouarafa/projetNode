pipeline {
    agent any

    stages {

        stage('Verify') {
            steps {
                echo "Jenkins is running!"
                echo "Branch: ${env.BRANCH_NAME}"
            }
        }

    }
}
