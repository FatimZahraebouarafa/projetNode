<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           targetNamespace="http://www.rendezvous-platform.com/message"
           xmlns:msg="http://www.rendezvous-platform.com/message"
           xmlns:rdv="http://www.rendezvous-platform.com/appointment"
           elementFormDefault="qualified">

    <!-- Import du schéma rendez-vous -->
    <xs:import namespace="http://www.rendezvous-platform.com/appointment"
               schemaLocation="rendezvous.xsd"/>

    <!-- Type d'expéditeur/récepteur -->
    <xs:simpleType name="userModelType">
        <xs:restriction base="xs:string">
            <xs:enumeration value="User"/>
            <xs:enumeration value="Professional"/>
        </xs:restriction>
    </xs:simpleType>

    <!-- Pièce jointe -->
    <xs:complexType name="attachmentType">
        <xs:sequence>
            <xs:element name="fileName" type="xs:string"/>
            <xs:element name="fileUrl" type="xs:anyURI"/>
            <xs:element name="fileType" type="xs:string"/>
            <xs:element name="fileSize" type="xs:integer"/>
        </xs:sequence>
    </xs:complexType>

    <!-- Message -->
    <xs:complexType name="messageType">
        <xs:sequence>
            <xs:element name="id" type="xs:ID"/>
            
            <!-- Rendez-vous associé -->
            <xs:element name="appointmentId" type="xs:IDREF"/> <!-- Référence à rendezvous.xsd -->
            
            <!-- Expéditeur -->
            <xs:element name="senderId" type="xs:IDREF"/>
            <xs:element name="senderModel" type="msg:userModelType"/>
            
            <!-- Récepteur -->
            <xs:element name="receiverId" type="xs:IDREF"/>
            <xs:element name="receiverModel" type="msg:userModelType"/>
            
            <!-- Contenu du message -->
            <xs:element name="content">
                <xs:simpleType>
                    <xs:restriction base="xs:string">
                        <xs:maxLength value="2000"/>
                    </xs:restriction>
                </xs:simpleType>
            </xs:element>
            
            <!-- Pièces jointes optionnelles -->
            <xs:element name="attachments" minOccurs="0">
                <xs:complexType>
                    <xs:sequence>
                        <xs:element name="attachment" type="msg:attachmentType" maxOccurs="unbounded"/>
                    </xs:sequence>
                </xs:complexType>
            </xs:element>
            
            <!-- Statut de lecture -->
            <xs:element name="isRead" type="xs:boolean" default="false"/>
            <xs:element name="readAt" type="xs:dateTime" minOccurs="0"/>
            
            <!-- Statut de suppression -->
            <xs:element name="isDeleted" type="xs:boolean" default="false"/>
            
            <!-- Horodatage -->
            <xs:element name="createdAt" type="xs:dateTime"/>
            <xs:element name="updatedAt" type="xs:dateTime"/>
        </xs:sequence>
    </xs:complexType>

    <!-- Conversation (groupe de messages liés à un rendez-vous) -->
    <xs:complexType name="conversationType">
        <xs:sequence>
            <xs:element name="appointmentId" type="xs:IDREF"/>
            <xs:element name="participants">
                <xs:complexType>
                    <xs:sequence>
                        <xs:element name="userId" type="xs:IDREF"/>
                        <xs:element name="professionalId" type="xs:IDREF"/>
                    </xs:sequence>
                </xs:complexType>
            </xs:element>
            <xs:element name="messages">
                <xs:complexType>
                    <xs:sequence>
                        <xs:element name="message" type="msg:messageType" maxOccurs="unbounded"/>
                    </xs:sequence>
                </xs:complexType>
            </xs:element>
            <xs:element name="lastMessageAt" type="xs:dateTime" minOccurs="0"/>
            <xs:element name="unreadCount" type="xs:integer" default="0"/>
            <xs:element name="isActive" type="xs:boolean" default="true"/>
        </xs:sequence>
    </xs:complexType>

    <!-- Élément racine -->
    <xs:element name="messagerie">
        <xs:complexType>
            <xs:choice>
                <xs:element name="message" type="msg:messageType" maxOccurs="unbounded"/>
                <xs:element name="conversation" type="msg:conversationType" maxOccurs="unbounded"/>
            </xs:choice>
        </xs:complexType>
    </xs:element>

    <!-- Notification de message -->
    <xs:complexType name="messageNotificationType">
        <xs:sequence>
            <xs:element name="messageId" type="xs:IDREF"/>
            <xs:element name="recipientId" type="xs:IDREF"/>
            <xs:element name="recipientModel" type="msg:userModelType"/>
            <xs:element name="senderName" type="xs:string"/>
            <xs:element name="previewText" type="xs:string"/>
            <xs:element name="notificationSent" type="xs:boolean" default="false"/>
            <xs:element name="sentAt" type="xs:dateTime" minOccurs="0"/>
        </xs:sequence>
    </xs:complexType>

</xs:schema>
